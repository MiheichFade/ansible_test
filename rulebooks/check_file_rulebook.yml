---
name: Minimal Always Trigger
hosts: []

rules:
  - Rule:
      name: Always fire
      condition:
        AllCondition: []   # Пустой список = условие всегда true
      actions:
        - Action:
            action: debug
            action_args: {}  # Пустой словарь, чтобы Rulebook не падал
