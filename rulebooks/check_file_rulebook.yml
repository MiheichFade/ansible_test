---
name: Always Trigger
hosts: []

rules:
  - Rule:
      name: Always Fire
      condition:
        AllCondition: []  # пустой список = всегда True
      actions:
        - Action:
            action: debug
            action_args:
              msg: "Rule triggered successfully"
