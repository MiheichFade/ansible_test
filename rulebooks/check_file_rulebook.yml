---
- name: minimal-rulebook
  hosts: localhost

  sources:
    - tick:
        interval: 10   # событие каждые 10 секунд

  rules:
    - name: periodic-debug
      condition: True
      action:
        debug:
          msg: "Событие tick сработало! Event: {{ event }}"
