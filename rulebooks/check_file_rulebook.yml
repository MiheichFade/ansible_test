---
name: Minimal Demo Rulebook
hosts:
  - localhost

sources: []

rules:
  - Rule:
      name: Always Trigger
      condition:
        AllCondition: []
      actions:
        - Action:
            action: debug
            action_args:
              msg: "This rule always triggers"
