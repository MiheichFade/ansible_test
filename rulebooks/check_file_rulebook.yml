---
name: Always Trigger
hosts: []

rules:
  - name: Always Fire
    condition:
      all: []        # пустой список = всегда True
    actions:
      - debug:
          msg: "Rule triggered successfully"
